# Pre-commit hooks configuration - Simplified for practical use
# See https://pre-commit.com for more information

repos:
  # Python formatting and linting with Ruff (replaces Black + flake8 + isort)
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.12.11
    hooks:
      - id: ruff-format
      - id: ruff-check
        args: [--fix]

  # Basic file checks
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.4.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-toml
      - id: check-json
      - id: check-merge-conflict
      - id: check-added-large-files
        args: [--maxkb=1000]
      - id: mixed-line-ending

  # Configuration file validation
  - repo: https://github.com/pre-commit/mirrors-prettier
    rev: v3.0.0
    hooks:
      - id: prettier
        types_or: [yaml, json, markdown]
        exclude: ^(\.github/|docs/|data/)
# Advanced checks (disabled for now - can be enabled later):
#
# Python security checks:
# - repo: https://github.com/PyCQA/bandit
#   rev: 1.7.5
#   hooks:
#     - id: bandit
#       args: [-r, src/, -f, json]
#       exclude: tests/
#
# Type checking:
# - repo: https://github.com/pre-commit/mirrors-mypy
#   rev: v1.5.0
#   hooks:
#     - id: mypy
#       args: [--ignore-missing-imports, --scripts-are-modules]
#       files: ^src/
#
# Tests:
# - repo: local
#   hooks:
#     - id: pytest
#       name: pytest
#       entry: pytest
#       language: system
#       types: [python]
#       args: [tests/, --tb=short]
#       pass_filenames: false
