---
description:
globs:
alwaysApply: true
---
# Cursor Rules for BNB Trading System

## 🎯 Project Type
Simple, modular BNB swing trading system with technical analysis. Target: **75%+ accuracy**.

## 🚀 Core Principles
- **Keep it simple** - no over-engineering
- **Modular design** - each indicator in separate file
- **Configuration-driven** - all parameters in config.toml
- **Easy to tune** - change settings without touching code

## 📊 Architecture
```
bnb_trading/
├── config.toml              # ALL PARAMETERS HERE
├── main.py                  # Entry point
├── data_fetcher.py          # Binance API with ccxt
├── fibonacci.py             # Fib calculations (separate module)
├── weekly_tails.py          # Weekly wick analysis (separate module)  
├── indicators.py            # RSI, MACD, BB (ta-lib)
├── signal_generator.py      # Combines all signals
├── validator.py             # Tracks accuracy
└── results.csv              # Signal history
```

## 🔧 Code Standards
- Python 3.8+ with type hints
- Use pandas, numpy, ccxt, ta-lib
- Handle NaN with `np.nan_to_num()`
- Log all operations
- Try/catch for API calls
- Save everything in CSV for analysis

## 📈 Signal Priority (from config)
1. **Fibonacci levels** - primary signals
2. **Weekly tails** - con

## 🚫 Забранява се
- **Хардкоднати тестови данни** - не прави DataFrame с фиксирани стойности
- **Тестови сигнали** - не прави dummy сигнали за тестване
- **Симулирани резултати** - не прави fake валидации
- **Тестови конфигурации** - всичко трябва да е в config.toml

## ✅ Правилно използване
- **main.py** - единствената точка за стартиране
- **Реални данни** - от Binance API чрез ccxt
- **Конфигурация** - всички параметри в config.toml
- **Логване** - всички операции се записват в лог файлове